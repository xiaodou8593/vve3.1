#$(module_prefix)_ride_on_nearest
# 聊天栏快捷乘坐
# 输入执行位置
# 以乘坐者为执行者

tag @e[tag=input] remove input
tag @s add input
execute as @e[tag=$(project_name)_$(module_name),limit=1,sort=nearest] run function $(module_prefix)_seat_on